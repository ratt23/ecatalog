# eCatalog Environment Variables

# Dashboard API URL (for local development)
VITE_DASHBOARD_API=http://localhost:8888/.netlify/functions

# Cloudinary Settings (optional, only needed if uploading images from eCatalog directly)
# VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
# VITE_CLOUDINARY_UPLOAD_PRESET=your-preset
